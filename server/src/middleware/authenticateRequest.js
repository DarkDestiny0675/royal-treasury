import{getUserById,verifyAccessToken}from"../services/authService.js";export function authenticateRequest(q,s,n){const[a,t]=(q.get("authorization")||"").split(" ");if(a!=="Bearer"||!t)return s.status(401).json({message:"Authentication is required."});try{const u=getUserById(verifyAccessToken(t).sub);if(!u)return s.status(401).json({message:"The account is unavailable."});q.user=u;n();}catch{return s.status(401).json({message:"The session has expired. Please sign in again."});}}
